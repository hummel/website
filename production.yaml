extends: site.yaml

mode: production
media_url: ./media
base_url: .
deploy_root: deploy_production

publisher:
    ssh:
        type: hyde.ext.publishers.ssh.SSH
        username: jhummel
        server: astro.as.utexas.edu
        target: www/